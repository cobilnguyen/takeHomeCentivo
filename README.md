Most of the work is boilerplate connecting to a database, just building up the connections. After that first I wanted to check if the id was valid, if the given id is not valid then there is no point in doing any more computations. Finally after validating the id I make the query, the reason I check for the age after making the query is because I wanted to separate the response from having a User exist but not be above the age of 21 and having the User not exist at all.